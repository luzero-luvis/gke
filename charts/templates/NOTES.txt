============================================================================
  mts-ijp — {{ include "mts-ijp.fullname" . }}
============================================================================

Service:    {{ include "mts-ijp.fullname" . }}
Namespace:  {{ include "mts-ijp.namespace" . }}
Image:      {{ .Values.image.name }}
{{- if .Values.autoscaling.enabled }}
Replicas:   {{ .Values.autoscaling.minReplicas }}-{{ .Values.autoscaling.maxReplicas }} (HPA-managed)
{{- else }}
Replicas:   {{ .Values.replicaCount }}
{{- end }}

{{- if .Values.service.enabled }}

IN-CLUSTER ADDRESS:
  http://{{ include "mts-ijp.fullname" . }}.{{ include "mts-ijp.namespace" . }}.svc.cluster.local:{{ .Values.service.port }}

  The images resolve each other by bare Service name, so the release names
  must stay: otp, gateway, backend, frontend, wa-bot.
{{- end }}

{{- if .Values.envFromSecret.enabled }}

SECRET:
  {{ .Values.envFromSecret.secretName }} (must exist — this chart does not create it)

  kubectl -n {{ include "mts-ijp.namespace" . }} get secret {{ .Values.envFromSecret.secretName }}
{{- end }}

{{- if .Values.podDisruptionBudget.enabled }}

DISRUPTION BUDGET:
  {{- if .Values.podDisruptionBudget.minAvailable }}
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  {{- end }}
  {{- if .Values.podDisruptionBudget.maxUnavailable }}
  maxUnavailable: {{ .Values.podDisruptionBudget.maxUnavailable }}
  {{- end }}
{{- end }}

{{- if .Values.autoscaling.enabled }}

AUTOSCALING:
  {{ .Values.autoscaling.minReplicas }}-{{ .Values.autoscaling.maxReplicas }} replicas
  {{- if .Values.autoscaling.targetCPUUtilizationPercentage }}, target CPU {{ .Values.autoscaling.targetCPUUtilizationPercentage }}%{{- end }}
  {{- if .Values.autoscaling.targetMemoryUtilizationPercentage }}, target memory {{ .Values.autoscaling.targetMemoryUtilizationPercentage }}%{{- end }}

  kubectl -n {{ include "mts-ijp.namespace" . }} get hpa {{ include "mts-ijp.fullname" . }}
{{- end }}

USEFUL COMMANDS:
  # Rollout status
  kubectl -n {{ include "mts-ijp.namespace" . }} rollout status deploy/{{ include "mts-ijp.fullname" . }}

  # Pods
  kubectl -n {{ include "mts-ijp.namespace" . }} get pods -l app.kubernetes.io/name={{ include "mts-ijp.name" . }}

  # Logs
  kubectl -n {{ include "mts-ijp.namespace" . }} logs -l app.kubernetes.io/name={{ include "mts-ijp.name" . }} --tail=50 -f

  # Port-forward
  kubectl -n {{ include "mts-ijp.namespace" . }} port-forward svc/{{ include "mts-ijp.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}

============================================================================
